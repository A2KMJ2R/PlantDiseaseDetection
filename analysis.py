import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files

# Example usage
folder_path = "C:\\Users\\mishe\\Downloads\\archive (5)\\New Plant Diseases Dataset(Augmented)\\New Plant Diseases Dataset(Augmented)\\train\\tomato"

num_files = count_files_in_folder(folder_path)
print(f"Number of files in the folder: {num_files}")

import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files

# Example usage
folder_path = "C:\\Users\\mishe\\Downloads\\archive (5)\\New Plant Diseases Dataset(Augmented)\\New Plant Diseases Dataset(Augmented)\\train\\strawberry"
num_files = count_files_in_folder(folder_path)
print(f"Number of files in the folder: {num_files}")

import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files

# Example usage
folder_path = "C:\\Users\\mishe\\Downloads\\archive (5)\\New Plant Diseases Dataset(Augmented)\\New Plant Diseases Dataset(Augmented)\\train\\potato"
num_files = count_files_in_folder(folder_path)
print(f"Number of files in the folder: {num_files}")

import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files

# Example usage
folder_path = "C:\\Users\\mishe\\Downloads\\archive (5)\\New Plant Diseases Dataset(Augmented)\\New Plant Diseases Dataset(Augmented)\\train\\corn"
num_files = count_files_in_folder(folder_path)
print(f"Number of files in the folder: {num_files}")

import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files

# Example usage
folder_path = "C:\\Users\\mishe\\Downloads\\archive (5)\\New Plant Diseases Dataset(Augmented)\\New Plant Diseases Dataset(Augmented)\\train\\apple"
num_files = count_files_in_folder(folder_path)
print(f"Number of files in the folder: {num_files}")

import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files


import os

def count_files_in_folder(folder_path):
    # Check if the folder exists
    if not os.path.exists(folder_path):
        print(f"The folder '{folder_path}' does not exist.")
        return
    
    # Get the list of files in the folder
    files = os.listdir(folder_path)
    
    # Count the number of files
    num_files = len(files)
    
    return num_files

# Example usage
folder_path = "C:\\Users\\mishe\\Downloads\\archive (5)\\New Plant Diseases Dataset(Augmented)\New Plant Diseases Dataset(Augmented)\\train\\tomato\\Tomato___Spider_mites Two-spotted_spider_mite"
num_files = count_files_in_folder(folder_path)
print(f"Number of files in the folder tomato_Spider_mites Two-spotted_spider_mite: {num_files}")










